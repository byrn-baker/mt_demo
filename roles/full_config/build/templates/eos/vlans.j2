{% if devices[0]["site"]["vlans"] is defined %}
   {% for vlan in devices[0]["site"]["vlans"] %}
      {% if vlan["vid"] == 4094 %}
vlan {{ vlan["vid"] }}
   name {{ vlan["name"] }}
   trunk group mlagpeer
!
      {% else %}
vlan {{ vlan["vid"] }}
   name {{ vlan["name"] }}
!
      {% endif %}
   {% endfor %}
{% endif %}