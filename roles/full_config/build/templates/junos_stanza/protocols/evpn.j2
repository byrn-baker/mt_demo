{% for vlan in devices[0]["site"]["vlans"] %}
    {% if vlan["vlan_vni"] != None %}
    evpn {
        encapsulation vxlan;
        extended-vni-list {{ vlan["vlan_vni"]}};
        }
    {% endif %}
{% endfor %}