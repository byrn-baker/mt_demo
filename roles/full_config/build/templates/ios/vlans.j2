{% if devices[0]["site"]["vlans"] is defined %}
    {% for vlan in devices[0]["site"]["vlans"] %}
        {% if vlan["name"] != 'MLAG_VLAN' %}
vlan {{ vlan["vid"] }}
 name {{ vlan["name"] }}
        {% endif %}
    {% endfor %}
{% endif %}