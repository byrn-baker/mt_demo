    evpn {
    {% if devices[0]["site"]["vlans"] != None %}
        encapsulation vxlan;
        extended-vni-list [{{ devices[0]["site"]["vlans"] | join(' ', attribute='vlan_vni') }}];
        multicast-mode ingress-replication;
        }
    {% endif %}